/*
 * Why typescript?
 * Catch common mistakes
 * Place constraints and add assumptions to code
 * Better DX
 */

//* Declare explicitly
let myFavoritePie: string = 'apple'
let numberOfPies: number = 5
let timeToEatPie: boolean = false
let pieTypes: string[] = ['pecan', 'blueberry', 'pumpkin']
let piesBought: {name: string; numberOfPies: number; favoritePie?: string}

// * Declare Implicitly
// * Reassign
// ! Change Type

// * Declare before assignment

// * View

export {}
